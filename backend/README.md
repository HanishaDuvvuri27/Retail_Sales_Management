

Tech Stack
- Node.js, Express
- MongoDB (primary) with in-memory fallback sample

Setup
- `cd backend`
- `npm install`
- Configure `MONGODB_URI` in `src/utils/db.js` or env
- `npm start` (or `npm run dev`)

